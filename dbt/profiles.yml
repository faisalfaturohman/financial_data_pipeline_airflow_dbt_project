financial_pipeline:
  outputs:
    dev:
      type: postgres
      host: postgres
      user: airflow
      password: airflow
      port: 5432
      dbname: airflow
      schema: analytics
      threads: 4
      keepalives_idle: 0
  target: dev